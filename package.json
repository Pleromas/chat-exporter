{
  "name": "chat-exporter",
  "version": "1.1.3",
  "private": true,
  "description": "Firefox extension that exports ChatGPT conversations to Markdown, JSON, HTML or PDF, entirely locally.",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "test": "node test-run.js && node test-security.js && node test-markdown.js",
    "lint": "web-ext lint",
    "sign": "web-ext sign --channel=unlisted",
    "build": "./build.sh"
  },
  "devDependencies": {
    "jsdom": "^30.0.0"
  },
  "webExt": {
    "ignoreFiles": [
      "package.json",
      "package-lock.json",
      "node_modules",
      "test-*.js",
      "out.html",
      ".git",
      ".gitignore",
      "CLAUDE.md",
      "build.sh"
    ]
  }
}
